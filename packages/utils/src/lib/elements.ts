export function getHeightpxsOf(element: HTMLElement) {
	return element.offsetHeight;
}

export function buildClassComponent(tokens:string[]):string{
	let res = ""
	for(let token of tokens ){
		if(!token || token == ""){
			
		}
	}
	return res;

}